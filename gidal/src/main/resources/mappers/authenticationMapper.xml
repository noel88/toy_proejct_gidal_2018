<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="org.gidal.mapper.AuthenticationMapper">


	<select id="user_login" resultType="org.gidal.user.domain.UserVO">
		select * from user where user_email = #{user_email} and user_password = #{user_password}
	</select>

	<select id="enterprise_login" resultType="org.gidal.enterprise.domain.EnterpriseVO">
		select * from enterprise where enterprise_email = #{enterprise_email} and enterprise_password = #{enterprise_password}
	</select>

	<update id="user_forget">
		update user set user_password = #{user_password} where user_email = #{user_email}
	</update>

	<update id="enterprise_forget">
		update enterprise set enterprise_password = #{enterprise_password} where enterprise_email = #{enterprise_email}
	</update>

	<delete id="user_delete">
		delete from user where user_email = #{user_email}
	</delete>

	<delete id="enterprise_delete">
		delete from enterprise where enterprise_email = #{enterprise_email}
	</delete>






</mapper>